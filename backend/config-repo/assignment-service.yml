server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dlm_assignment_db
    username: root
    password: Prakhar@321

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  application:
    name: assignment-service

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/