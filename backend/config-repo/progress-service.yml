server:
  port: 8083

spring:
  application:
    name: progress-service

  datasource:
    url: jdbc:mysql://localhost:3306/progress_db
    username: root
    password: Prakhar@321

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"